// The enum lives in this user-created stub file so existing Unity metadata stays intact.
// The filename is slightly unusual for an enum-only file, but the type itself is the
// clean shared payload other day/night scripts should reference.
public enum DayNightPhase
{
    Sunrise,
    Day,
    Sunset,
    Night
}
